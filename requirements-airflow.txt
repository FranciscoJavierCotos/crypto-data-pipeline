# Pinned runtime dependencies for reproducible Airflow image builds.
# dbt versions are pinned to versions already observed in project run artifacts.
dbt-databricks==1.11.6
databricks-sql-connector>=4.1.3

# Keep provider resolved by Airflow constraints file tied to AIRFLOW_VERSION + Python version.
apache-airflow-providers-databricks
