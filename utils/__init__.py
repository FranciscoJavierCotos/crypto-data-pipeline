# ---------------------------------------------------------------------------
# Shared utilities package for the crypto data pipeline.
# Mounted at /opt/airflow/utils inside Docker containers.
# Requires /opt/airflow on PYTHONPATH (set in docker-compose.yaml).
# ---------------------------------------------------------------------------
